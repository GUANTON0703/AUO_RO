services:
  rotxt:
    image: rotxt:latest
    container_name: rotxt
    restart: unless-stopped
    env_file: /srv/rotxt/rotxt.env
    volumes:
      - /srv/rotxt/data:/data
    ports:
      # 綁 docker bridge gateway：NPM 容器連得到，但 LAN / 外網進不來（走 NPM 反代）
      - "172.17.0.1:8010:8000"
