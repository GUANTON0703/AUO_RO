# 客戶端 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development。
> **審核政策**：不跑 codex。分批做完跑 `uv run pytest -q` + 掃 diff；階段收尾一次自行審核。

**Goal:** `rich` 做的終端機客戶端，接上所有伺服器 API。做完 `uv run python -m server` + `uv run python -m client` 就能真的玩——註冊/登入、建角色、掛機看事件滾動、加點轉職、背包商店精煉。

**Architecture:** REPL 為主，不做 always-on Live 儀表板（Windows 終端機 + 阻塞輸入相容性差）。每個指令後印一次 `rich` 狀態面板。`hunt` 後自動進「觀看模式」：每 5 秒 poll `/api/hunt/status`、印新事件，按 Enter 停止觀看（掛機在伺服器繼續）。低頻操作（加點、精煉、商店…）走引導式選單。

**Tech Stack:** `rich`（已在依賴）、`httpx`（已在 dev 依賴，移到主依賴）。

---

## 設計決策

1. **不做 Live 分割面板**：REPL + 每次指令後 `render_status()`。`hunt` 進 watch loop（poll + 按 Enter 停）。理由：`rich.Live` 跟阻塞式 `input()` 在 Windows 衝突，watch loop 用背景 thread 讀 stdin 設旗標最穩。
2. **API client 可測**：`client/api.py` 的 `ApiClient` 收一個 `httpx.Client`（或相容物）。測試用 `httpx.Client(transport=httpx.ASGITransport(app=create_app()), base_url="http://test")` 直接打 FastAPI app，不起真 server。
3. **token 持久化**：存 `~/.rotxt/session.json`（`{server_url, token, username}`）。`--server URL` 參數可覆寫。
4. **一帳號一「當前角色」**：伺服器的 hunt/shop/inventory 端點都對「帳號第一隻角色」操作（前面階段這樣設計）。客戶端建/選角色後，若不是第一隻，提示「v1 掛機固定操作第一隻角色」——或客戶端建角色時就只讓建一隻。**v1 簡化：客戶端只顯示/操作第一隻角色**，多角色列 v1.5（要動伺服器端把 character_id 帶進端點）。
5. **事件顯示**：`kill_batch` → 一行「擊殺 波利 ×N　+經驗 +Zeny」；`rare_drop` → `★` 開頭單獨一行；`retreat` → 紅字；戰鬥引擎的 `attack`/`skill` 事件（在線逐場才有）→ 縮排灰字，可用設定 `verbose` 開關。

---

## 檔案結構

```
client/
  __init__.py
  __main__.py         # python -m client：解析 --server，進 app.run()
  config.py           # 讀寫 ~/.rotxt/session.json
  api.py              # ApiClient：包所有端點，帶 token，錯誤轉例外
  render.py           # rich 渲染：status_panel / exp_bar / inventory_table / event_lines
  auth_flow.py        # 登入/註冊/建角色的互動流程
  watch.py            # 掛機觀看模式（poll loop + 按 Enter 停）
  menus.py            # 加點/技能/裝備/精煉/鑲嵌/商店/倉庫/轉職 引導式選單
  app.py              # 主 REPL：指令分派
scripts/play.py       # 起 server subprocess + 開 client（本機一鍵玩）
.claude/launch.json   # server 設定，供 preview_start
tests/
  test_client_api.py
  test_client_render.py
  test_client_flow.py
```

---

## Task 1: ApiClient

**Files:** Modify `pyproject.toml`（httpx 移主依賴）; Create `client/__init__.py`, `client/api.py`; Test `tests/test_client_api.py`

- [ ] **Step 1: `pyproject.toml`** 把 `httpx` 從 `[dependency-groups].dev` 移到 `[project].dependencies`（`rich` 也確認在主依賴，沒有就加 `rich>=13`）。`uv sync`。

- [ ] **Step 2: 寫失敗測試 `tests/test_client_api.py`**

```python
import httpx
import pytest

from client.api import ApiClient, ApiError
from server.app import create_app
from server.db import connection


@pytest.fixture
def api(tmp_path):
    connection.configure(str(tmp_path / "c.db"))
    connection.init_db()
    from server.auth import invites
    code = invites.create_invite()
    transport = httpx.ASGITransport(app=create_app())
    http = httpx.Client(transport=transport, base_url="http://test")
    return ApiClient(http), code


def test_register_login_flow(api):
    client, code = api
    client.register(code, "player1", "password123")
    client.login("player1", "password123")
    assert client.token is not None


def test_create_and_list_character(api):
    client, code = api
    client.register(code, "p2", "password123")
    client.login("p2", "password123")
    ch = client.create_character("英雄王")
    assert ch["name"] == "英雄王"
    assert client.list_characters()[0]["name"] == "英雄王"


def test_api_error_on_bad_login(api):
    client, code = api
    with pytest.raises(ApiError):
        client.login("nobody", "nope")


def test_hunt_start_status_stop(api):
    client, code = api
    client.register(code, "p3", "password123")
    client.login("p3", "password123")
    client.create_character("掛機王")
    client.hunt_start("prontera_east_gate")
    st = client.hunt_status()
    assert "kills" in st
    client.hunt_stop()


def test_shop_and_inventory(api):
    client, code = api
    client.register(code, "p4", "password123")
    client.login("p4", "password123")
    ch = client.create_character("購物狂")
    inv = client.inventory(ch["id"])
    assert "items" in inv and "equipment" in inv
    shop = client.shop()
    assert any(i["id"] == "red_potion" for i in shop["items"])
```

- [ ] **Step 3: 寫 `client/api.py`**

```python
class ApiError(Exception):
    def __init__(self, status: int, detail: str):
        super().__init__(f"[{status}] {detail}")
        self.status = status
        self.detail = detail


class ApiClient:
    def __init__(self, http, token: str | None = None):
        self._http = http
        self.token = token

    def _headers(self):
        return {"Authorization": f"Bearer {self.token}"} if self.token else {}

    def _req(self, method: str, path: str, **kw):
        resp = self._http.request(method, path, headers=self._headers(), **kw)
        if resp.status_code >= 400:
            try:
                detail = resp.json().get("detail", resp.text)
            except Exception:
                detail = resp.text
            raise ApiError(resp.status_code, str(detail))
        return resp.json() if resp.content else None

    # --- auth ---
    def register(self, invite_code, username, password):
        return self._req("POST", "/api/accounts",
                         json={"invite_code": invite_code, "username": username, "password": password})

    def login(self, username, password):
        body = self._req("POST", "/api/sessions", json={"username": username, "password": password})
        self.token = body["token"]
        return body

    def logout(self):
        self._req("DELETE", "/api/sessions")
        self.token = None

    # --- characters ---
    def list_characters(self): return self._req("GET", "/api/characters")
    def create_character(self, name): return self._req("POST", "/api/characters", json={"name": name})
    def allocate_stats(self, cid, deltas): return self._req("POST", f"/api/characters/{cid}/stats", json=deltas)
    def learn_skill(self, cid, skill_id, level):
        return self._req("POST", f"/api/characters/{cid}/skills", json={"skill_id": skill_id, "level": level})
    def jobchange(self, cid, target): return self._req("POST", f"/api/characters/{cid}/jobchange", json={"target_job_id": target})
    def reset_stats(self, cid): return self._req("POST", f"/api/characters/{cid}/resetstats")
    def reset_skills(self, cid): return self._req("POST", f"/api/characters/{cid}/resetskills")

    # --- hunt ---
    def hunt_start(self, map_id, monster_id=None):
        b = {"map_id": map_id}
        if monster_id: b["monster_id"] = monster_id
        return self._req("POST", "/api/hunt/start", json=b)
    def hunt_status(self): return self._req("GET", "/api/hunt/status")
    def hunt_stop(self): return self._req("POST", "/api/hunt/stop")

    # --- inventory / shop / storage ---
    def inventory(self, cid): return self._req("GET", f"/api/characters/{cid}/inventory")
    def equip(self, cid, inst_id): return self._req("POST", f"/api/characters/{cid}/inventory/equip", json={"equipment_instance_id": inst_id})
    def unequip(self, cid, slot): return self._req("POST", f"/api/characters/{cid}/inventory/unequip", json={"slot": slot})
    def socket(self, cid, inst_id, card_id): return self._req("POST", f"/api/characters/{cid}/inventory/socket", json={"equipment_instance_id": inst_id, "card_item_id": card_id})
    def refine(self, cid, inst_id): return self._req("POST", f"/api/characters/{cid}/inventory/refine", json={"equipment_instance_id": inst_id})
    def shop(self): return self._req("GET", "/api/shop")
    def buy(self, item_id, qty=1): return self._req("POST", "/api/shop/buy", json={"item_id": item_id, "qty": qty})
    def sell(self, item_id=None, qty=1, equipment_instance_id=None):
        b = {"qty": qty}
        if item_id: b["item_id"] = item_id
        if equipment_instance_id: b["equipment_instance_id"] = equipment_instance_id
        return self._req("POST", "/api/shop/sell", json=b)
    def storage(self): return self._req("GET", "/api/storage")
    def deposit(self, **b): return self._req("POST", "/api/storage/deposit", json=b)
    def withdraw(self, **b): return self._req("POST", "/api/storage/withdraw", json=b)
```

> 若某端點的實際路徑/欄位跟上面對不上（前面階段實作時可能微調過），以伺服器實際為準，跑測試對齊。

- [ ] **Step 4: 跑通過** PASS
- [ ] **Step 5: Commit** `git commit -m "feat: 客戶端 ApiClient"`

---

## Task 2: 設定與登入流程

**Files:** Create `client/config.py`, `client/auth_flow.py`; Test `tests/test_client_flow.py`

- [ ] **Step 1: 寫失敗測試 `tests/test_client_flow.py`**

```python
from client.config import SessionStore


def test_session_store_roundtrip(tmp_path):
    store = SessionStore(tmp_path / "s.json")
    store.save(server_url="http://x", token="tok", username="u")
    loaded = SessionStore(tmp_path / "s.json").load()
    assert loaded["token"] == "tok" and loaded["username"] == "u"


def test_session_store_missing_returns_none(tmp_path):
    assert SessionStore(tmp_path / "none.json").load() is None
```

> `auth_flow` 是互動流程（`rich.Prompt`），難純測——最少測 `SessionStore`。`auth_flow` 的分支邏輯（有存檔 → 試 token；token 失效 → 重登；無存檔 → 問註冊/登入）用 monkeypatch `Prompt.ask` 做一個 happy-path 測試即可。

- [ ] **Step 2: 跑失敗** → FAIL

- [ ] **Step 3: 實作**
  - `client/config.py`：`SessionStore(path)` 有 `.load() -> dict|None` / `.save(**fields)`。預設 path = `Path.home() / ".rotxt" / "session.json"`。
  - `client/auth_flow.py`：`ensure_logged_in(api: ApiClient, store: SessionStore, server_url: str) -> None`
    - 有存檔 → 設 `api.token`，試 `api.list_characters()`，成功就回
    - 失敗/無存檔 → 問「1. 登入　2. 用邀請碼註冊」→ 對應流程 → 存檔
  - `select_or_create_character(api) -> dict`：`list_characters()`，沒有就問名字建一隻；有就回第一隻（v1 只操作第一隻）

- [ ] **Step 4: 跑通過**
- [ ] **Step 5: Commit** `git commit -m "feat: 客戶端設定與登入流程"`

---

## Task 3: rich 渲染

**Files:** Create `client/render.py`; Test `tests/test_client_render.py`

- [ ] **Step 1: 寫失敗測試 `tests/test_client_render.py`**

```python
from rich.console import Console

from client.render import status_panel, event_lines, inventory_table


def _render(renderable) -> str:
    con = Console(width=80, record=True)
    con.print(renderable)
    return con.export_text()


def test_status_panel_shows_key_fields():
    ch = {"name": "英雄", "job_id": "swordman", "base_level": 25, "job_level": 15,
          "base_exp": 1200, "job_exp": 300, "zeny": 5000,
          "hp": 900, "max_hp": 1000, "sp": 40, "max_sp": 60,
          "location_map": "prontera_south_field"}
    out = _render(status_panel(ch))
    assert "英雄" in out and "25" in out and "5000" in out


def test_event_lines_formats_kill_batch_and_rare():
    events = [
        {"kind": "kill_batch", "monster_name": "波利", "count": 42, "base_exp": 84,
         "job_exp": 42, "zeny": 210},
        {"kind": "rare_drop", "item_id": "poring_card", "item_name": "波利卡片", "qty": 1},
        {"kind": "retreat", "reason": "補品用盡", "seconds_survived": 3600},
    ]
    lines = event_lines(events)
    text = "\n".join(str(l) for l in lines)
    assert "波利" in text and "42" in text
    assert "★" in text            # 稀有掉落
    assert "補品用盡" in text


def test_inventory_table_lists_items_and_equipment():
    inv = {"items": {"red_potion": 10, "jellopy": 55},
           "equipment": [{"id": 1, "equipment_id": "knife", "refine": 7,
                          "equipped_slot": "weapon", "card_ids": []}]}
    out = _render(inventory_table(inv))
    assert "red_potion" in out and "10" in out
    assert "knife" in out and "+7" in out
```

- [ ] **Step 2: 跑失敗** → FAIL

- [ ] **Step 3: 實作 `client/render.py`**
  - `status_panel(character: dict) -> Panel`：職業中文名、Base/Job 等級 + 經驗條（用下一級所需經驗算百分比——client 端可 import `server.progression.levels.base_exp_for_next` / `job_exp_for_next`）、HP/SP 條、Zeny、地點
  - `event_lines(events: list[dict]) -> list[str|Text]`：照設計決策 §5 格式
  - `inventory_table(inv: dict) -> Table` / `shop_table` / `storage_table`
  - `hunt_summary(status: dict) -> Panel`：本次結算的擊殺/經驗/Zeny/是否離線/是否撤退

- [ ] **Step 4: 跑通過**
- [ ] **Step 5: Commit** `git commit -m "feat: 客戶端 rich 渲染"`

---

## Task 4: 掛機觀看模式

**Files:** Create `client/watch.py`; Test（手動為主，最小自動測）

- [ ] **Step 1: 寫 `client/watch.py`**

```python
import threading
import time

from rich.console import Console

from client.api import ApiClient
from client.render import event_lines, hunt_summary


def watch_hunt(api: ApiClient, console: Console, poll_seconds: float = 5.0) -> None:
    console.print("[dim]掛機中。按 Enter 停止觀看（掛機在伺服器繼續）。[/dim]")
    stop = threading.Event()

    def _wait_enter():
        try:
            input()
        except EOFError:
            pass
        stop.set()

    threading.Thread(target=_wait_enter, daemon=True).start()

    while not stop.is_set():
        try:
            status = api.hunt_status()
        except Exception as exc:
            console.print(f"[red]結算失敗：{exc}[/red]")
            break
        if status.get("retreated"):
            console.print(hunt_summary(status))
            console.print("[yellow]已撤退，掛機結束。[/yellow]")
            break
        for line in event_lines(status.get("events", [])):
            console.print(line)
        stop.wait(poll_seconds)
    console.print("[dim]停止觀看。[/dim]")
```

- [ ] **Step 2: 最小測試**——monkeypatch `api.hunt_status` 回固定 status（含 retreated=True）+ monkeypatch `input` 立刻回，斷言迴圈會結束、印出 summary。

- [ ] **Step 3: Commit** `git commit -m "feat: 客戶端掛機觀看模式"`

---

## Task 5: 引導式選單

**Files:** Create `client/menus.py`; Test `tests/test_client_flow.py`（追加，monkeypatch Prompt）

每個選單一個函式，收 `api` + `character`，用 `rich.prompt.Prompt` / `IntPrompt` 問，呼叫對應 API，印結果：
- `stats_menu`：顯示目前六屬性 + 可用點數，問「加哪個、加幾點」→ `api.allocate_stats`
- `skills_menu`：列本職技能（從 `server.content` 讀）+ 已學等級 + 技能點，問學哪個到幾級 → `api.learn_skill`
- `equip_menu`：列背包裝備，問穿哪件 → `api.equip`；再問要不要卸下某 slot
- `refine_menu`：列可精煉裝備 + 目前精煉值 + 需要的礦/Zeny + 成功率，確認 → `api.refine`，印成功/失敗降級
- `socket_menu`：列有空孔的裝備 + 背包的卡 → `api.socket`
- `shop_menu`：列商店，問買什麼幾個 / 賣什麼 → `api.buy` / `api.sell`
- `storage_menu`：列倉庫 + 背包，存/取 → `api.deposit` / `api.withdraw`
- `jobchange_menu`：檢查 job_level，列可轉職業 → `api.jobchange`

- [ ] **Step 1: 寫測試**——挑 2~3 個選單（`stats_menu` / `shop_menu` / `refine_menu`）用 monkeypatch `Prompt.ask` 餵輸入、用 fake api 記錄呼叫，斷言呼叫正確的 API 帶正確參數。
- [ ] **Step 2: 跑失敗 → 實作 → 跑通過**
- [ ] **Step 3: Commit** `git commit -m "feat: 客戶端引導式選單"`

---

## Task 6: 主 REPL 與進入點

**Files:** Create `client/app.py`, `client/__main__.py`, `scripts/play.py`, `.claude/launch.json`

- [ ] **Step 1: `client/app.py`**——`run(server_url: str)`:
  1. `SessionStore`、`httpx.Client(base_url=server_url)`、`ApiClient`
  2. `ensure_logged_in` → `select_or_create_character`
  3. 印歡迎 + `render_status`
  4. REPL：讀指令，分派：
     - `h` / `hunt` → 選地圖（列解鎖的）→ 選怪（列該圖的怪 + 等級）→ `api.hunt_start` → `watch_hunt`
     - `stop` → `api.hunt_stop` + 印 summary
     - `s` / `status` → 重新 `api.list_characters()` + `render_status`；若在掛機也 `hunt_status` 一次
     - `i` / `inv` → `inventory_table`
     - `stats` / `skills` / `equip` / `refine` / `socket` / `shop` / `storage` / `job` → 對應 menu
     - `help` → 指令表
     - `q` / `quit` → 存檔、離開
  5. 任何 `ApiError` 印紅字、不崩

- [ ] **Step 2: `client/__main__.py`**——`argparse --server`（預設讀存檔的 server_url，再預設 `http://127.0.0.1:8000`）→ `app.run(server_url)`

- [ ] **Step 3: `scripts/play.py`**——`subprocess.Popen([sys.executable, "-m", "server"])`，等 `/health` 通，再 `app.run("http://127.0.0.1:8000")`，結束時關 server。

- [ ] **Step 4: `.claude/launch.json`**
```json
{"version": "0.0.1", "configurations": [
  {"name": "rotxt-server", "runtimeExecutable": "uv",
   "runtimeArgs": ["run", "python", "-m", "server"], "port": 8000}
]}
```

- [ ] **Step 5: 手動 e2e**——起 server、`uv run python -m client`：註冊 → 建角色（看起手裝）→ 買 50 紅藥 → hunt 南門原野 → 看事件滾動幾輪 → stop → 加點 → 精煉小刀 → 穿上 → 再 hunt。截圖/貼過程。
- [ ] **Step 6: Commit** `git commit -m "feat: 客戶端主 REPL 與進入點"`

---

## Task 7: 自行審核與文件

- [ ] **Step 1: 全套測試** `uv run pytest -q` 全綠
- [ ] **Step 2: 自行審核** 讀 `client/` 全部，重點：
  - `ApiClient` 每個方法的路徑/欄位跟伺服器實際端點一致（跑一次真 server 對）
  - watch 模式的 thread 收得乾淨（daemon thread，退出不卡）
  - `ApiError` 到處被接住，不會因為一個 400 整個 client 崩
  - token 失效（過期）時 auth_flow 會重登不會無限迴圈
  - 經驗條百分比算對（用 client import 的 `base_exp_for_next`）
  - Windows 終端機下 rich 輸出正常（中文、emoji、box 字元）
- [ ] **Step 3: 更新 `README.md`**——加「## 玩」章節：`uv run python -m server` + `uv run python -m client`，或 `uv run python scripts/play.py`
- [ ] **Step 4: 更新 `docs/superpowers/plans/README.md`** 標記階段 10 完成，備註「本機可玩達成」
- [ ] **Step 5: Commit** `git commit -m "docs: 客戶端；本機可玩里程碑達成"`

---

## Self-Review（寫計畫時）

**1. 對照設計文件 §14：**
- rich TUI ✓
- 上半狀態、下半指令 → 用「每指令後印面板 + watch 模式滾動」近似（見設計決策 §1，記進抽象清單）
- 常用指令短（h/s/i）✓
- 轉職/加點/精煉走引導式選單 ✓
- 客戶端散布：發 .py + `uv run python -m client` ✓（README）

**2. Placeholder scan：** Task 5 / Task 6 用條列描述選單與 REPL 分派——每個選單的輸入→API 對應明確，pattern 一致（Prompt → api.X → 印結果）。Task 1 Step 3 給了完整 `ApiClient`，其他 Task 的渲染/流程函式介面在測試裡定義了簽名與預期輸出。

**3. Type consistency：**
- `ApiClient` 方法 ↔ 伺服器端點（前 7 階段）——路徑在此列出，實作時對齊
- `status_panel(character: dict)` 的 dict 形狀 ↔ `/api/characters` 回傳一致
- `event_lines(events)` 的 event dict `kind` ↔ 結算器事件（第 4 階段 `dataclasses.asdict`）一致
- `hunt_status()` 回傳形狀 ↔ 第 5 階段 hunt.py 的 response 一致

**4. 已知後續：**
- 多角色操作 → v1.5（要伺服器端把 character_id 帶進 hunt/shop 端點）
- 真 Live 分割面板 → 之後（prompt_toolkit 或 textual）
- 網頁前端 → v1.5（同一套 ApiClient 概念，換 fetch）
- MVP 挑戰、社交（排行/交易/聊天）的客戶端介面 → 等第 8、9 階段做完再加對應選單
